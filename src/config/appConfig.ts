const config = {
  version: '0.0.1',
  contextMemu: {
    enabled: false,
    hostname: ['tauri.localhost', 'localhost'],
  },
  initPath: '/playground',
  dateFormat: 'LLLL',
  modal: {
    zIndex: 1001,
  },
};

export default config;
